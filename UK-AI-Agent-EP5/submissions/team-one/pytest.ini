# Scope a bare `pytest` (run from team-one/) to the real suite in tests/.
# Without this, pytest recurses into archive/v0.1, whose same-named test files
# and old bouncer.py shadow the live modules and break collection.
[pytest]
testpaths = tests
