{
  "_comment": "User base / profile store. 'users' maps a name -> role; 'roles' defines each role's department + the access categories it may see (RBAC). The walking skeleton reads name->role->department for profile tags; the access categories (allow/deny/*) are here ready for when the real Bouncer snaps back in. JSON has no comments, so this '_comment' key is ignored.",

  "categories": [
    "schedules",
    "opening-hours",
    "goods-weights-volumes",
    "financials",
    "legal",
    "shared"
  ],

  "roles": {
    "driver": {
      "department": "logistics",
      "allow": ["schedules", "opening-hours", "goods-weights-volumes", "shared"]
    },
    "exec": {
      "department": "leadership",
      "allow": ["*"],
      "deny": ["legal"]
    }
  },

  "users": {
    "bob": "driver",
    "alice": "exec"
  }
}
