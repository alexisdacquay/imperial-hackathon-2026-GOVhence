{
  "_comment": "M3 stub directory: who the users are and what each ROLE may see. The loader (memory.py) turns a role into the exact SET of allowed categories the bouncer checks against. JSON has no real comments, so this leading '_comment' key is ignored by the loader.",

  "categories": [
    "schedules",
    "opening-hours",
    "goods-weights-volumes",
    "financials",
    "legal",
    "shared"
  ],

  "roles": {
    "driver": {
      "allow": ["schedules", "opening-hours", "goods-weights-volumes", "shared"]
    },
    "exec": {
      "allow": ["*"],
      "deny": ["legal"]
    }
  },

  "users": {
    "bob": "driver",
    "alice": "exec"
  }
}
